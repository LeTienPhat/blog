<div class="container mt-4">
  <% content_for :title, "Editing Post" %>

  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="card">
        <div class="card-header">
          <h1 class="h4 mb-0">Edit Post</h1>
        </div>
        <div class="card-body">
          <%= render "form", url: users_post_path(@post), post: @post %>
        </div>
        <div class="card-footer">
          <%= link_to "Show this post", users_post_path(@post), class: "btn btn-info me-2 text-white" %>
          <%= link_to "Back to posts", users_posts_path, class: "btn btn-secondary text-white" %>
        </div>
      </div>
    </div>
  </div>
</div>
